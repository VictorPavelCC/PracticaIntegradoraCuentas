    <style>
      body { background: darkorange; 
      margin: 0; 
      display: flex; 
      justify-content: center; 
      align-items: center; }

      .product-info{
      display: flex;
      flex-direction: column;
      align-items: center;
      font-size: 20;
      padding: 10px;
      }
      .product-details {
      padding: 20px;
      background-color: #fff;
      border-radius: 30px;
      display:flex;} 

      .product-image { max-width: 100%;}
      .product-image-container{
      display: flex;
      flex-direction: column;
      align-items: center;
      }
      .add-to-cart-btn {      
      background-color: #206cf8;
      margin-top: 10px;
      color: #ffffff; 
      border: none; 
      border-radius: 25px; 
      width: 50%; 
      height: 50px; 
      font-size: 1.5rem; 
      }
    </style>


  <body>
    <div class="container mt-5">
      <div class="product-details">
        <div class="product-info">
          <h1>{{payload._doc.name}}</h1>
          <p><strong>Categoría:</strong> {{payload._doc.category}}</p>
          <p><strong>Stock:</strong> {{payload._doc.stock}}</p>
          <p><strong>Precio:</strong> ${{payload._doc.price}}</p>
        </div>
        <div class="product-image-container">
          <img
            src="{{payload._doc.image}}"
            alt="{{payload._doc.name}}"
            class="product-image"
          />
          <button
            class="add-to-cart-btn"
            onclick="addToCart(this)"
            id="{{payload._doc._id}}"
          >
            <i class="bi bi-cart-plus"></i>
          </button>
        </div>
      </div>
    </div>

